#include <iostream>
#include <vector>
using namespace std;

int getMinDistance(vector<int> nums, int start, int target){
    for(int i = 0; i < nums.size(); i++){
        cout << nums;
    }
}

int main(){
    
    vector<int> nums = {1, 2, 3, 4, 5};
    int start = 2;
    int target = 5;

    getMinDistance(nums, start, target);

    return 0;
}