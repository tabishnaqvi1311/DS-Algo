#include <iostream>
using namespace std;
//uses the "divide and conquer principle"

//divide the array into single element arrays
//repeatedly merge these smaller arrays until the array is sorted
//time complexity is 0(nlogn)

//it is implemented recursively
//first find the mid element
//BASE CASE: stop when the array length is less than 2
//break the array into two halves
//and recurse the function on both halves
int main(){
    
}